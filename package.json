{
  "name": "ctor-exp",
  "version": "0.0.0",
  "description": "Experimental implementation of the constructor concepts discussed in https://blog.dwac.dev/posts/constructor/.",
  "main": "dist/ctor.js",
  "types": "dist/ctor.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig-lib.json",
    "build:test": "tsc -p tsconfig-test.json",
    "test": "npm run -s clean && npm run -s build:test && jasmine --config=jasmine.json",
    "clean": "rm -rf dist/ test/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dgp1130/ctor-exp.git"
  },
  "keywords": [
    "constructor",
    "experiment",
    "language"
  ],
  "author": "Doug Parker",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dgp1130/ctor-exp/issues"
  },
  "homepage": "https://github.com/dgp1130/ctor-exp#readme",
  "devDependencies": {
    "@types/jasmine": "^3.6.0",
    "jasmine": "^3.6.2",
    "typescript": "^4.0.5"
  }
}
